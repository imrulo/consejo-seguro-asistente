# Infraestructura y DevOps

Este directorio contendrá archivos para la infraestructura, despliegue y automatización:

- `docker-compose.yml` (próximamente): Orquestación de servicios (backend, base de datos, Redis, etc.)
- Configuración de CI/CD (GitHub Actions)
- Scripts de despliegue y monitoreo

---

## Próximos pasos
- Añadir `docker-compose.yml` para desarrollo local.
- Configurar workflows de CI/CD para backend y frontend.
- Documentar variables de entorno y secrets.
